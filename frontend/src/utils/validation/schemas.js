import { tripSchema } from "@/utils/validation/tripSchema";
import { planDetailSchema } from "@/utils/validation/planDetailSchema";

//バリデーションスキーマをまとめて定義

export const schemas = {
  tripSchema,
  planDetailSchema,
};
